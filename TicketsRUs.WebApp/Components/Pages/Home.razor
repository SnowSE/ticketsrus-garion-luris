@page "/"
@using QRCoder
@using System.Drawing
@using System.Drawing.Imaging

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

@if (dataLoaded)
{
    <img src="@qrCodeString" />
}


@code {
    string qrCodeString = "";
    bool dataLoaded = false;
    Bitmap test;


    // protected override async Task OnInitializedAsync()
    // {
    //     Bitmap qr = QRCodeToolkit.GenerateQRCodeFromString("This is a testing string");
    //     byte[] qrCodeArray = QRCodeToolkit.ConvertToByteArray(qr);
    //     UpdateStringData(qrCodeArray);

    //     dataLoaded = true;
    //     StateHasChanged();
    // }

    // private void UpdateStringData(byte[] data)
    // {
    //     qrCodeString = $"data:image;base64,{Convert.ToBase64String(data)}";
    }
}